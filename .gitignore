# 运行时产物（启动器日志、临时文件）
launch.log
*.log

# 自测截图输出（每次跑测试都会重新生成，不入库）
docs/verify/

# 依赖与构建产物（本项目零依赖，这里只是防御性忽略）
node_modules/
build/

# 编辑器 / 系统
.vscode/
.idea/
.DS_Store
Thumbs.db
desktop.ini
*.swp
*~

# 打包产物（由 build-standalone / build-release 生成，作为 Release 附件上传，不入库）
dist/
# 同上：发布 zip 与解压后的成品目录。二进制附件放 Git 历史里会让仓库迅速膨胀，
# 而且 git 无法对 zip 做有效 diff/压缩。要发布就传到 Release 附件。
#
# 注意必须写成 `release/*` 而不是 `release/`：一旦整个目录被排除，
# git 会直接剪掉该目录、无法再用 `!` 把里面的文件捞回来（这是 git 的既定行为）。
# 用 `release/*` 只排除目录内容，才能保留 release/README.md。
release/*
!release/README.md
