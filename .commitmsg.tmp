修复联机同步与表现层共 8 个问题（用户实测反馈）

用户一次报了 10 个问题，本次修复其中 8 个（另 2 个待确认根因）。全部先定位根因再改。

1. 队友看不到地上掉落物（含自己生成的）
根因两处：
  a) session.js 的 EV.ENEMY_DEATH 只在「击杀者本机」生成掉落（原注释认为
     "各端各自生成结果一致"，但实际只有击杀者那端会生成，队友永远看不到）
  b) main.js 的 _onEnemyKill 在 source 是字符串（房客击杀）时直接 return，
     房主既不结算也不生成掉落 —— 房客击杀的敌人一件东西都不掉
修法：掉落列表统一由房主生成并随单局状态广播完整列表，客机按 uid 替换应用。
  · 替换语义而非增量：队友捡走、物品过期都会自然同步，列表自愈不留幽灵掉落
  · 位置 set 原地更新，避免每 0.5 秒重建 Float32Array 的持续分配
  · 可行前提：敌人掉落表由 enemy.id 决定（确定性），任务奖励掉落本就只在房主侧结算

2 + 3. 队友无法按 E 做任务 / 任务进度不共享
根因：run.js 的 _updateObjectives 只检查房主自己的玩家对象 p 和单一开关
objectiveInteractDown，队友输入对房主完全不可见；客机又在本地各算各的会分歧。
修法：
  · protocol.js 新增 FLAG.INTERACT / FLAG.HEALING，客机把按住交互上报
  · session.js 把上报值写到远程代理（interacting / healingActive / netId）
  · main.js 新增 _objectiveInteractors()：单机为本机玩家，联机房主追加各房客代理
  · run.js 遍历所有交互者推进进度，o.interactBy 标记谁在推（供 HUD 显示）
  · 只有房主计算进度（authoritativeObjectives），客机只显示广播值

8. 撤离
根因同 2/3：_updateExtraction 只用房主自己的位置判断是否在信标内。
修法：任何一位在信标范围内的玩家都能推进撤离读条，客机以房主广播为准。

6. 队友治疗不可见
HEALING 标志随玩家状态同步，队友打药时各端可见（配合状态条）。

7. 击中/跑动烟雾过大
原 smoke 0.35->1.5m、dust 0.28->1.05m、explosion 0.55->2.6m、muzzleSmoke 0.16->0.85m，
击中与落地时正好在准心前方炸开一大团、糊住视野。整体缩到约 1/4 并略降 alpha。
注意基础表和调用处的 size/sizeEnd 覆盖值都要改，只改一处不生效。

9. 观战视角卡在任务模型里
观战相机原本直接坐在队友眼球位置，近裁剪面切进身边的任务模型/墙体。
改为沿视线前移 0.35m，既脱出模型又与第一人称观感一致。

10. 队友进房，房主界面毫无反应
根因：_applyRoster 只把"xxx 加入了房间"写进 lastEvent，而 lastEvent 仅通过
debugState() 暴露给 F3 调试面板，玩家侧没有任何提示；同时 HUD 的 _paintMenu()
只在 showMenu() 时跑一次，面板打开后队友再加入也不刷新，表现为列表一直空的。
实测服务器侧推送正常（房主确实收到含两人的 roster），问题纯在表现层。
修法：
  · _applyRoster 主动播报加入/离开（带当前人数 x/N），加入时配提示音
  · setLanState 在面板打开时立即重绘；HUD render() 每帧补绘联机面板，
    面板保持打开时队友加入也能实时刷新

顺带修正：
· _updateObjectives 的交互判定兼容旧开关 objectiveInteractDown，保留既有自测与
  外部调用的可用性（test-objectives 恢复 5/5）
· test-auto-cert 改为等 server 关闭后再定退出码，修掉退出时的 libuv 断言崩溃
  （Windows 上表现为 0xC0000409）